package com.set.guide.forms;

import javax.swing.*;

public class HomePage extends InitGui{
    private JPanel mainPanel;

    public HomePage(String title) {
        super(title);
        start_gui(mainPanel);
    }
}
